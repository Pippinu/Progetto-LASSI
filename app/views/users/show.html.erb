<%= content_tag :h1, current_user.welcome %>
Current user id : <%= current_user.id %>
<%= link_to "Sign Out", sessions_destroy_path(:id => current_user.id), data: {
                      turbo_method: :delete,
                      turbo_confirm: "Are you sure?"
                  }, :class => "btn btn-danger" %>